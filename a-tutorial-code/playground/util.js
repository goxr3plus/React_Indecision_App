console.log("Loaded util.js");

 export const square = x => x * x;
 export const add = (a, b) => a + b;
 export default (a, b) => a - b;

// const square = x => x * x;
// const add = (a, b) => a + b;
// const substract = (a, b) => a - b;

// export { square, add as default, substract as subby };
