import React from "react";
import ReactDOM from "react-dom";

const template = <p> This is JSX FROM WEBPACK </p>; // React.createElement("p", {}, "testing");
ReactDOM.render(template, document.getElementById("app"));   //
